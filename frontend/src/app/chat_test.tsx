// pages/WebSocketTest.js

import WebSocketClient from './socket';

const WebSocketTest = () => {
  return (
    <div>
      <h1>WebSocket Test</h1>
      <WebSocketClient />
    </div>
  );
};

export default WebSocketTest;
